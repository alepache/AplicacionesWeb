<%= form_for(@raza) do |f| %>
  <% if @raza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raza.errors.count, "error") %> prohibited this raza from being saved:</h2>

      <ul>
      <% @raza.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :raza_nombre %><br>
    <%= f.text_field :raza_nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
